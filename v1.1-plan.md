# Active Graph v1.1 — consolidated backlog

Generated by the post-v1.0.3 contract review (2026-05-19). This
plan supersedes the scattered "filed for v1.1" markers in
CONTRACT.md amendments from v0.5 onward. CONTRACT.md now points
here instead of carrying inline forward-references.

This is a **backlog, not a roadmap**. Priority tiers help
sequencing; "v1.1 must include X" is not the framing. The plan
collects every concrete v1.1 candidate raised across milestone
amendments and assigns each: title, description, source amendment,
dependencies, priority tier, scope estimate.

CONTRACT v1.1 expands this into a proper milestone scope when v1.1
work begins. Until then, the plan is the visibility layer for what
is owed.

---

## Open questions before v1.1 work begins

These bottleneck the consolidation pass — answer before scoping
the milestone:

1. **Is `graph.query` deprecated in v1.1, or carried indefinitely?**
   v1.0.3 #1 carved out "no deprecation in v1.0.3." The choice
   shapes API-DEP-1 below.
2. **Is `Runtime.load` auto-provider a "v1.1 must" or a "v1.x maybe"?**
   The rc1 user-test surfaced friction (B2); the rc2 fix was
   tactical. The decision shapes RUNTIME-1 below.
3. **CONTRACT modularization (DOC-1 / §7 #5 of findings):**
   split-per-milestone, executive-summary preambles, or no
   change? The decision is process; it does not block code work
   but shapes the v1.1 amendment-cycle workflow.
4. **Test-file naming sweep (§7 #6 of findings):** rename
   `test_v1_0_3_*` and similar version-tagged files? Cosmetic;
   landing in v1.1 is the natural moment.

---

## Priority tiers

Each entry carries one of:

- **must-have** — closes a documented adoption-surface gap; the
  framework's coherence depends on it.
- **should-have** — closes a discipline gap or a deferred
  follow-on with shipped fixes in adjacent work.
- **nice-to-have** — ergonomics, harmonization, or polish whose
  absence does not block adoption.

Tiers are review-time assessments, not commitments. v1.1 scoping
re-prioritizes against capacity.

---

## Backlog by theme

### Theme A — Error-completeness milestone (the largest, oldest backlog)

The v1.0 PR-A-through-PR-G series ended with ~30 items deferred.
Final tally per PR-G end-of-series note (CONTRACT v1.0 §PR-G):
22 partial Pack* migrations, 4 DB-error wrappers, plus the rc1
user-test gate carryover (now superseded by shipped rc2/v1.0.1
work). Re-applying the events-not-exceptions principle (CONTRACT
v1.0 #4b) is part of the milestone — some partials may turn out to
be event-shaped on second look.

#### A-1 — Complete the 22 partial Pack* migrations

**Description**: 22 of 28 Pack* registration-error raise sites are
unmigrated — they use the legacy single-message
`ActiveGraphError.__init__` branch (format-noncompliant but valid).
PR-E migrated only the 2 high-value `loader.py` sites. The classes
are in the v1.0 hierarchy; messages catch up incrementally.

**Source**: CONTRACT v1.0 §PR-E "Partial migration note"; restated
in §PR-F, §PR-G "v1.1 error-completeness milestone scope."

**Dependencies**: none.

**Priority**: should-have. Each unmigrated raise produces a
format-noncompliant error message; users hitting one of the 22
sites get the v0.9-shaped message instead of the v1.0 structured
form.

**Scope**: medium. Mostly mechanical, but each migration involves a
"what's the actual recovery prose" decision per site. Reverse-audit
order (hardest first) per the v1.0 PR-E discipline.

#### A-2 — Migrate the 4 deferred DB-error wrappers

**Description**: SQLite `sqlite3.OperationalError` and Postgres
`psycopg.OperationalError` variants. Per CONTRACT v1.0 §PR-C
flagged-not-migrated: multiple distinct failure modes (lock
timeout, journal corruption, disk full, auth, host unreachable,
db missing, conn dropped). Each needs per-mode recovery prose.

**Source**: CONTRACT v1.0 §PR-C "Flagged-not-migrated"; restated
in §PR-G "v1.1 error-completeness milestone scope."

**Dependencies**: needs a contributor with hands-on DB-failure
experience; the PR-C discipline note rejected making up prose.

**Priority**: should-have. Without it, real production failures
bubble unwrapped driver exceptions.

**Scope**: medium-to-large. 6-10 distinct failure modes across
two drivers, each with its own recovery prose.

#### A-3 — Re-audit Pack* migrations under the events-not-exceptions principle

**Description**: per CONTRACT v1.0 §PR-G series-completion note,
some of A-1's 22 partial migrations may be event-shaped on second
look (e.g., `PackPromptLoadError` firing during pack reload after
the runtime is up may belong as `behavior.failed` rather than as
a raised exception). Apply CONTRACT v1.0 #4b as the audit instrument.

**Source**: CONTRACT v1.0 §PR-G series-completion note.

**Dependencies**: A-1 (re-audit happens before migration locks
in).

**Priority**: should-have.

**Scope**: small (audit), then folds back into A-1's scope per
finding.

---

### Theme B — Provider commitment surface (v1.0.1 / v1.0.2 / v1.0.3 carryovers)

#### B-1 — OpenAI tool-shape translation in `Tool.to_definition()`

**Description**: `Tool.to_definition()` emits the Anthropic
`{name, description, input_schema}` shape. OpenAI's chat-completions
API expects `{type:"function", function:{name, description,
parameters}}` and returns `tool_calls` rather than `tool_use`
content blocks. `OpenAIProvider.complete()` currently raises
`LLMBehaviorError(reason="llm.network_error")` with a v1.1 pointer
when `tools=` is non-empty.

v1.1 scope: provider-aware tool-shape translation (either
parameterize `Tool.to_definition(provider="anthropic"|"openai")`
or push the shape into the provider's `complete()`), parallel
`tool_calls` extraction in `OpenAIProvider`, parity tests across
both providers for the same tool-using behavior.

**Source**: CONTRACT v1.0.1 #5(c) clause 2; restated in v1.0.3 #4
"OpenAI tool support is out of scope" and in v1.1 #7-and-beyond.

**Dependencies**: none.

**Priority**: must-have. The current state ships two providers
with asymmetric tool support; that undermines v1.0.1 #5's
provider-commitment claim for tool-using workloads.

**Scope**: large. New `Tool.to_definition(provider=...)` shape,
new `OpenAIProvider` tool-extraction path, parity-test
infrastructure, error-semantics alignment with v0.6 #11 reason
taxonomy.

#### B-2 — Native structured-output mode for providers that support it

**Description**: both shipped providers use the framework's
instruction-based path (schema + example instance in system prompt
per v1.0.1 #2). OpenAI's `response_format={"type":"json_schema",...}`
mode forces the model to emit conforming JSON natively, eliminating
the schema-echo failure mode v1.0.1 #2 patched in the prompt.

v1.1 scope: opt-in flag on `@llm_behavior` (or per-provider config)
toggling native mode, cache-key impact analysis (prompt-hash inputs
change because the system prompt shrinks), replay-determinism
guarantees across the mode flip.

**Source**: CONTRACT v1.0.1 #5(c) clause 3; restated in v1.0.3
"v1.1 backlog candidates" and v1.1 #7-and-beyond.

**Dependencies**: B-1 desirable (same provider-aware codepath).

**Priority**: should-have. Closes a stated non-promise from
v1.0.1; not blocking adoption today.

**Scope**: medium. Mostly cache-key and replay-determinism
analysis; the API surface is small.

#### B-3 — Pack-load-time cross-provider validation

**Description**: pack behaviors loaded after Runtime construction
via `load_pack` currently fall through to first-run validation in
`_ensure_registry()` rather than load-pack-time validation. v1.0.2.post1
fired validation at the decorator-time and Runtime-construction
binding moments, but did not extend to `load_pack` because that
binding moment surfaces a different ordering.

**Source**: CONTRACT v1.0.2 #1(b); restated in v1.0.3 "v1.1
backlog candidates" #5.

**Dependencies**: none.

**Priority**: should-have. Closes the third binding moment named
in v1.0.2 #1(b). Today a pack with a cross-provider mismatch fails
lazily; users with packs see the lazy path.

**Scope**: small. The validator factored out in v1.0.2.post1's
`activegraph/runtime/_live.py` is reusable; the work is integrating
it into `load_pack`'s call path.

#### B-4 — Dict-form `output_schema=` support

**Description**: users reasonably pass JSON-schema dicts to
`output_schema=` because that's the de facto cross-provider
structured-output format. v1.0.3 #2 strict-validates at decoration
time (rejects dicts with a structured TypeError + example). v1.1
opens the design question of whether to accept dicts via a
runtime converter or via a dedicated `output_schema_dict=` kwarg.

Open design questions per CONTRACT v1.0.3 #2:
- Which providers' structured-output APIs accept dicts natively?
- What is the equivalence contract between dict-form and
  Pydantic-form schemas?
- What is the prompt-hash treatment for fixtures (the dict is
  hashable; the Pydantic class is not, today)?
- Should the framework grow `LLMProvider.recognizes_schema_dict()`
  as a Protocol method (mirroring v1.0.2 #1's
  `recognizes_model()`)?

**Source**: CONTRACT v1.0.3 #2 "v1.1 candidate" + "v1.1 backlog
candidates."

**Dependencies**: design pass needed before implementation.

**Priority**: should-have. v1.0.3 #2 closed the user's pain
(silent failure with confusing diagnostic) via early failure; the
ergonomics gain is real but not blocking.

**Scope**: large. The decision matrix above each maps to
implementation choices; the wrong choice ships a feature that
fights with the framework's existing schema model.

#### B-5 — `on_failure=` callback on `run_goal()` and siblings

**Description**: v1.0.3 #3 added two failure-observation surfaces
(WARNING log, `Runtime.errors`). A first-class
`run_goal(..., on_failure=callback)` parameter is a larger
affordance, deferred to v1.1.

Open design questions per CONTRACT v1.0.3 #3:
- Synchronous vs asynchronous callback shape?
- Whether the callback can suppress the `behavior.failed` event
  entirely (likely no — events are the audit trail).
- Whether `on_failure=` composes with explicit
  `@behavior(on=['behavior.failed'])` subscribers (they should
  coexist, but the ordering needs locking).

**Source**: CONTRACT v1.0.3 #3 "v1.1 candidate" + "v1.1 backlog
candidates."

**Dependencies**: none.

**Priority**: nice-to-have. The v1.0.3 #3 affordances already give
callers structured access; the callback is ergonomics.

**Scope**: medium. The callback wiring is small; the design pass
is where the work lives.

#### B-6 — Fire-once-on-condition aggregation triggers

**Description**: from second user-test report (finding E): LLM
behaviors subscribed to high-volume event streams want
"fire-once-when-conditions-are-met" semantics. The user worked
around with a "gate behavior" pattern — a Python `@behavior`
creates a control object exactly once when conditions are met; the
LLM behavior subscribes to the control object.

v1.1 scope: name the pattern, expose it as a first-class
decorator argument (or a `@trigger` helper), document the
gate-behavior pattern as the fallback when the trigger doesn't
fit.

**Source**: CONTRACT v1.0.3 §"v1.0.3 deliberately does NOT touch"
Finding E; restated in v1.1 backlog candidates #4.

**Dependencies**: none.

**Priority**: should-have. Real user friction surfaced this; the
workaround is documented but not ergonomic.

**Scope**: medium. The API shape design is the hard part; the
implementation is a layer over existing scheduling.

#### B-7 — `Runtime.load` auto-provider ergonomics

**Description**: rc1 user-test surfaced B2: `Runtime.load(...)`
crashed with `MissingProviderError` when the loaded diligence pack
had LLM behaviors and the caller didn't pass `llm_provider=`. The
rc2 tactical fix passes `RecordedDiligenceProvider` explicitly; v1.1
opens the design question of whether `Runtime.load` should infer
a provider from the run's recorded events, a pack-manifest
declaration, or a sidecar in run metadata.

Open design questions per CONTRACT v1.1 #5:
- `auto_provider=True` flag on `Runtime.load` (opt-in inference).
- Pack-declared default provider.
- Sidecar in run metadata.

Replay-strict interaction must lock alongside the choice.

**Source**: CONTRACT v1.1 #5; user-test B2 finding.

**Dependencies**: none.

**Priority**: should-have. Adoption-surface friction surfaced by a
real user test.

**Scope**: medium. The chosen shape determines scope; sidecar is
largest, `auto_provider=True` flag is smallest.

---

### Theme C — API surface coherence (from this review)

#### C-1 — Deprecate `graph.query` / `object_type=` kwarg

**Description**: v1.0.3 #1 added `graph.objects(type=, where=)` as
canonical; kept `graph.query(object_type=, where=)` as a
backward-compatible alias with explicit "no deprecation in v1.0.3"
carve-out. The kwarg name divergence (`type` vs `object_type`)
between canonical and alias is internally inconsistent.

v1.1 scope: emit `DeprecationWarning` on `graph.query` calls and
on `object_type=` kwarg use. Update remaining callers (internal
`tools/graph_query.py`, pack examples, docs).

**Source**: CONTRACT v1.0.3 #1 "Why not deprecate graph.query in
v1.0.3"; review §2 Finding C.

**Dependencies**: none.

**Priority**: should-have. The longer the alias lives, the harder
the eventual removal.

**Scope**: small.

#### C-2 — Full Graph/View surface harmonization

**Description**: review §2 Findings A, B, F. `graph.relations()`
absent (also a v1.0.4 candidate as Finding A); `graph.events` is
a property while `view.events(type=...)` is a method;
`graph.all_objects()` / `all_relations()` coexist with
`graph.objects()` / a hypothetical `graph.relations()`.

v1.1 scope: lock a Graph-side method shape that mirrors View
exactly for read access (`graph.objects`, `graph.relations`,
`graph.events` all callable with optional `type=`/`where=`),
deprecate the no-arg variants (`all_objects`, `all_relations`,
`events` property), preserve `get_object` / `get_relation` /
`get_relations` for endpoint-shaped queries.

**Source**: review §2 Findings A, B, F. Finding A is also v1.0.4
candidate v1.0.4 #1 — the smallest piece can ship sooner.

**Dependencies**: a deprecation cycle, so should land alongside C-1.

**Priority**: should-have.

**Scope**: medium. The API surface is small; the impact on
existing callers is medium.

#### C-3 — Lock failure-routing for eval-time pattern failures

**Description**: surfaced during v1.0.4 #2's audit of which
per-error pages get the `failure-model.md` footer.

The framework's existing convention: errors raised inside behavior
dispatch (`_invoke` / `_invoke_llm` / `_invoke_tool`) are caught
and emitted as `behavior.failed` events; the dispatch wrapper is
`except Exception` at `runtime.py:738` plus explicit `except`
clauses for `LLMBehaviorError` and `ToolError`. Asymmetric to that
convention, two error classes that fire during dispatch are
deliberately *outside* any try/except:

- **`ReplayDivergenceError`** (`runtime.py:978`, inside
  `_invoke_llm`'s strict-replay hash check). The framework treats
  it as a fixture-management programmer error and lets it
  propagate to the caller of `run_until_idle()`.
- **`UnsupportedPatternError`** eval-time raises
  (`patterns.py:852, 902`) reached via `registry.match()` called
  from `_loop` at `runtime.py:583` with no try/except wrapper —
  they propagate out of `run_until_idle()` the same way.

This asymmetry may be intentional (some errors are programmer
errors that should fail loud; some are runtime conditions that
should be observable as events and continued past) or it may be
drift. v1.1 should lock the decision either way:

- **Option A — keep the asymmetry, lock it in CONTRACT.** Amend
  v0.6 #11 (or v1.0 #4b) to name which error classes deliberately
  escape vs. route via `behavior.failed`, with the criterion
  ("programmer-error class" vs. "runtime-condition class").
- **Option B — route everything dispatch-time via behavior.failed.**
  Wrap `registry.match()` and the strict-replay hash check in
  `_emit_behavior_failed` paths; users observe these through
  `Runtime.errors` like every other failure.

Either is defensible. The current state is neither documented nor
locked.

**Source**: v1.0.4 #2 audit (see commit history); review §3
Finding I noted no doc pages still imply failures crash the run,
but did not audit dispatch-time escape paths.

**Dependencies**: none (CONTRACT amendment + possibly small code
change to route paths uniformly).

**Priority**: should-have. The asymmetry isn't blocking adoption
but it is the kind of latent drift Standing Rule §2 was adopted
to catch — and an undocumented "programmer-error class" carve-out
is exactly the kind of unwritten rule that surfaces as a user-test
finding later.

**Scope**: small (amendment alone) → small-medium (amendment +
the wrap-and-emit code path if Option B wins).

---

### Theme D — CI gate completeness (deferred from earlier milestones)

#### D-1 — Spec-vs-impl drift gate for CLI flags

**Description**: catch CLI flag references in docs that don't
resolve to real flags in code. The `--set` gap (and the `--memo` /
`--search` gaps it revealed) shipped through v1.0-rc1 because the
gate didn't exist.

**Source**: CONTRACT v1.1 #2; also CONTRACT review §7 #2(b) and
Standing rule §2 in the CONTRACT review banner — the mechanical
complement to the boundary-anchored test discipline.

**Dependencies**: none.

**Priority**: **must-have** (raised from should-have post-review).
The contract review explicitly elevated this. Rationale: the
boundary-mismatch class (v0.5 #8 `_requeue_unfired`, v1.0.2 #1(b)
lazy validation) recurs whenever the contract names a boundary
that the implementation's path diverges from. Amendment-cycle
discipline (Standing rule §2) catches some cases; mechanical
checking catches the rest. The drift gate is what makes the
discipline auditable.

**Scope**: small.

#### D-2 — Executable-Python-snippet drift gate

**Description**: extension of D-1 covering executable Python
snippets in tutorial and cookbook pages. Catches the v1.0-rc1
finding B2 shape — documented Python code that crashes because the
shape drifted from shipped behavior.

v1.0-rc2 landed `tests/test_tutorial_snippets.py` as a tactical
down-payment; v1.1 generalizes the pattern.

**Source**: CONTRACT v1.1 #2 expansion; same drift-discipline
framing as D-1.

**Dependencies**: D-1 (same gate-shape, can share infrastructure).

**Priority**: **must-have** (raised from should-have post-review,
same rationale as D-1).

**Scope**: medium. The opt-in annotation shape and the test-runner
isolation are the harder parts.

#### D-3 — Version-tag-correspondence CI gate

**Description**: existing version-sync gate
(`tests/test_version_sync.py`) asserts
`activegraph.__version__` matches `pyproject.toml`'s version. Both
can be wrong together. The v1.0-rc1 user-test caught
`activegraph --version` returning `0.9.1` after rc1 publish; the
v1.1 gate closes the loop by asserting correspondence with the
current annotated git tag in tagged-release CI runs.

**Source**: CONTRACT v1.1 #6.

**Dependencies**: none.

**Priority**: must-have. Direct credibility hit if it recurs.

**Scope**: small.

#### D-4 — Package-data completeness gate (the wheel)

**Description**: rc2 user-test finding B3: published wheel omitted
`activegraph/packs/diligence/prompts/*.md`, every fresh
`pip install` crashed in quickstart. Source-tree tests couldn't
catch this. The v1.1 gate builds the wheel, installs into fresh
venv, runs `activegraph quickstart` end-to-end.

**Source**: CONTRACT v1.1 #8. Already shipped in v1.0-rc3 as
`tests/test_wheel_completeness.py` + workflow; needs required-status
flip on main for merge-blocking.

**Status**: shipped; required-status-check toggle is a
maintainer-owned operational step.

**Priority**: must-have.

**Scope**: nil (shipped). Operational gate enablement only.

#### D-5 — Deploy-verification gate

**Description**: rc2/rc3 user-test finding B4: doc site at the
declared URL returned 404 because GitHub Pages was never enabled.
v1.1 gate fetches `DOCS_BASE_URL` + known page paths, asserts HTTP
200 + content marker.

**Source**: CONTRACT v1.1 #9. Already shipped in v1.0-rc3 as
`tests/test_doc_site_reachable.py` + workflow.

**Status**: shipped (gate runs on `main` push + cron + PR trigger
added in v1.0 final). Red until Pages is enabled + DNS configured;
that's the correct signal per the rc3 entry. Required-status flip
on main is gated on Pages going live.

**Priority**: must-have.

**Scope**: nil (shipped). Operational steps owned externally.

---

### Theme E — Discipline ratchets (gates that lock current quality)

#### E-1 — Type-completeness on the public-surface allowlist

**Description**: CONTRACT v1.0 #C5 baselined `mypy --strict` at
22/38 allowlist modules clean. `docs/reference/api/TYPE_REPORT.md`
enumerates the 16 dirty modules with per-module error categories.
v1.1 closes the gap in a PR series.

Categories per the report:
- mechanical: `no-untyped-def`, `type-arg`, `no-any-return`.
- judgment: `attr-defined`, `arg-type`, `misc`.

**Source**: CONTRACT v1.1 #3.

**Dependencies**: none.

**Priority**: should-have. Discipline ratchet; prevents
regressions on already-clean surface.

**Scope**: medium. 3-5 commits grouped by category.

#### E-2 — Docstring-completeness on the public-surface allowlist

**Description**: CONTRACT v1.0 #C2 baselined Ring 0 docstring
coverage at 92/100. v1.1 has two waves:
- Wave 1: close 8 Ring 0 "missing" gaps (one-liners minimum).
- Wave 2: upgrade every Ring 0 one-line docstring to a "full"
  docstring (≥3 lines, Args/Returns/Raises/Examples).
Ring 1 burndown follows.

**Source**: CONTRACT v1.1 #4.

**Dependencies**: none. Co-paths with E-1.

**Priority**: should-have. Same discipline-ratchet framing as E-1.

**Scope**: medium for Wave 1; large for Wave 2.

#### E-3 — Ring 0 docstring exemption sweep

**Description**: close the 8 Ring 0 exemptions in
`docstring_gaps.toml`. End state: exemption list empty; gate
passes against 100% Ring 0 not-missing.

**Source**: HANDOFF.md "Known v1.1 scope" CONTRACT v1.1 #4 Wave 1;
included for completeness.

**Dependencies**: subsumed by E-2 Wave 1.

**Priority**: see E-2.

**Scope**: see E-2.

---

### Theme F — CLI implementation gaps (rc1-surfaced)

#### F-1 — `activegraph fork --set <pack>.<key>=<value>`

**Description**: override a pack setting on a fork at the CLI.
Most user-visible of the three rc1 CLI gaps; rendered in
`cookbook/common-patterns.md`, `concepts/forking.md`,
`reference/cli.md`. Tutorial step 7's fork-and-diff closer
depends on it. Python-API workaround documented at
`docs/cookbook/common-patterns.md#fork-with-a-pack-setting-override-v10-python-api`.

Open design questions per CONTRACT v1.1 #1:
- Replay semantics for overridden forks.
- Error timing (fork-time vs load-time).
- Type coercion failure.
- Schema migration on the `runs` row.

**Source**: CONTRACT v1.1 #1.

**Dependencies**: none.

**Priority**: must-have. Documented behavior with no shipped
implementation is a credibility hit.

**Scope**: medium-to-large. Schema migration plus replay-semantics
choice plus pack-import-at-CLI-time tradeoff.

#### F-2 — `activegraph inspect --memo`

**Description**: render a run's memo objects via the same renderer
the quickstart command uses (lift `_print_memo_section` from
`activegraph/cli/quickstart.py` into a shared module).

**Source**: CONTRACT v1.1 #1 (transcript-spec'd, not user-facing).

**Dependencies**: none.

**Priority**: should-have. Spec'd, not currently visible to users;
shipping unblocks the spec-completeness narrative.

**Scope**: small.

#### F-3 — `activegraph inspect --search "<query>"`

**Description**: substring / keyword search across a run's events.
Spec'd in transcript BEAT 3.

**Source**: CONTRACT v1.1 #1.

**Dependencies**: none.

**Priority**: should-have. Same framing as F-2.

**Scope**: small. Lightest of the three.

---

### Theme G — Internal-evaluator and patch-lifecycle cleanups

#### G-1 — Internal-evaluator errors needing framework-version context

**Description**: per HANDOFF.md and CONTRACT v1.0 PR-G, two
internal-evaluator cases needed framework-version context.

**Status**: PR-G closed this — v1.0 PR-G's "Internal-bug
consistency pass" landed the shared
`activegraph.errors.internal_bug_fields(...)` helper across all
three internal-bug sites (patterns.py x2, graph.py x1). The v1.1
"error-completeness milestone scope" tally explicitly marks this
as **closed by PR-G**.

**Priority**: nil (closed).

**Scope**: nil.

Listed here for completeness so future readers don't surface it
again from the old HANDOFF.md "Known v1.1 scope" entry.

---

### Theme H — Fork / cache semantics

#### H-1 — Fork cache pre-population symmetry

**Description**: `Runtime.fork(at_event=...)` in-process
pre-populates the LLM cache from the parent's recorded
`llm.responded` events (per CONTRACT v0.6 #8). The persistent
shape (`SQLiteEventStore.fork_run` → `Runtime.load(run_id=fork)`)
does NOT — the loaded fork sees only the fork's events, ending at
the fork point.

v1.1 scope: `Runtime.load` of a fork run should look up the
parent's `forked_at_event_id` (already on the `runs` row) and
pre-populate the cache from the parent's events through that
point. New runtime behavior, banned in v1.0.

**Source**: CONTRACT v1.1 #7-and-beyond.

**Dependencies**: none.

**Priority**: should-have. Asymmetry surfaced in rc2 #4/8 B2 fix;
fork workflows in production hit it.

**Scope**: medium.

---

### Theme I — Documentation discipline (from this review)

#### I-1 — CONTRACT modularization decision

**Description**: CONTRACT.md is 5595 lines (post-review banner) and
has been growing ~800-1200 lines per patch release. New readers
cannot cover it cover-to-cover without a guided path. Three options
per review §7 #5:
- Split per-milestone files (`CONTRACT/v0.5.md`, etc.) with a
  master index.
- Add executive-summary preambles per milestone (TL;DR ~5-10
  lines).
- Lean on doc-site synthesis pages (`failure-model.md` is the
  in-place example); CONTRACT stays decision-record only.

**Source**: review §7 #5.

**Dependencies**: open question above ("CONTRACT modularization").

**Priority**: must-have for v1.1 phase 1 (raised from should-have
post-review). Rationale: subsequent v1.1 amendments will be easier
to file cleanly against a modular contract. Landing modularization
late in v1.1 means every interim v1.1 amendment compounds the
length problem before the fix lands; landing early means the
v1.1 amendment cycle benefits from the cleaner structure as it
runs.

**Scope**: small (decision) → medium (split or preamble pass).

#### I-2 — Boundary-anchored test discipline in project docs

**Description**: review §7 #2 surfaced boundary-mismatch as a
recurring class (v0.5 #8, v1.0.2 #1). The v1.0.2.post1 tests in
`test_llm_default_model.py` Section (g) are the canonical model.
The CONTRACT-side half landed in this review as Standing rule §2
in the banner. I-2 is the docs-side half: surface the discipline
in `HANDOFF.md` ("Discipline patterns" section) or a new
`docs/about/discipline.md` page so contributors (human or agent)
who don't read CONTRACT cover-to-cover still encounter the rule
at the testing-strategy decision point.

**Source**: review §7 #2; CONTRACT banner Standing rule §2 is the
in-CONTRACT half already shipped by this review.

**Dependencies**: none.

**Priority**: should-have. Process-quality fix; complements the
mechanical drift gates (D-1, D-2) which are the must-have side.

**Scope**: small.

#### I-3 — Test-file naming sweep

**Description**: rename version-tagged test files like
`test_v1_0_3_behavior_failed_ux.py` to feature-named files
(`test_runtime_errors.py` or `test_behavior_failure_ux.py`).
Purely cosmetic; v1.1 is the natural moment.

**Source**: review §7 #6.

**Dependencies**: none.

**Priority**: nice-to-have.

**Scope**: small.

#### I-4 — Cross-link `replay-divergence-error.md` to replay/fixture documentation

**Description**: surfaced during v1.0.4 #2's audit of which
per-error pages get the `failure-model.md` footer.
`ReplayDivergenceError` is the counterintuitive case: it fires
during behavior dispatch (`runtime.py:978`, inside `_invoke_llm`'s
strict-replay hash check) but the raise sits deliberately outside
any try/except, so it propagates to the caller of
`run_until_idle()` rather than emitting `behavior.failed`. The
standard v1.0.4 #2 footer ("see `Runtime.errors` and
`BehaviorFailure`") would mislead — `Runtime.errors` will not
contain a `ReplayDivergenceError`.

The page needs its own cross-link to wherever replay / fixture-
management is canonically documented (`docs/concepts/replay.md`
or the equivalent — verify it exists; if not, the doc page itself
may need to be added). The cross-link replaces the standard
behavior-failed footer for this page specifically.

Filed separately from v1.0.4 #2 because (a) v1.0.4 #2 is
mechanical-footer-only by scope; (b) the cross-link target needs
verification or creation, which is more than a one-line edit;
(c) the question of *why* `ReplayDivergenceError` escapes rather
than routing through `behavior.failed` is a contract decision
(see C-3), and the cross-link's phrasing depends on how C-3 is
resolved.

**Source**: v1.0.4 #2 audit.

**Dependencies**: ideally lands after C-3 so the page reflects the
locked routing convention.

**Priority**: should-have. The page is one of 31 per-error pages;
the standard footer wasn't applied; users who land on it from a
strict-replay failure don't currently get a clear next step.

**Scope**: small (one-page edit + verify cross-link target).

---

## Suggested grouping into v1.1 phases (optional sequencing)

Phase ordering is a planning aid, not a commitment. Each phase
lists tier-priority items first; nice-to-have items follow within
phase. v1.1 scoping can choose any subset.

### Phase 1 — Discipline foundations + coherence must-haves

Drift gates and modularization first: the rest of the v1.1
amendments file more cleanly against them. Bumped from later
phases post-review per CONTRACT review banner Standing rules §1
and §2.

- I-1 CONTRACT modularization decision + execution (must-have for
  phase 1, small → medium) ← bumped from Phase 4. Subsequent v1.1
  amendments file cleaner against a modular contract.
- D-1 CLI-flag drift gate (must-have, small) ← bumped from Phase 2.
  Mechanical complement to Standing rule §2.
- D-2 Python-snippet drift gate (must-have, medium) ← bumped from
  Phase 2.
- D-3 Version-tag-correspondence gate (must-have, small).
- B-1 OpenAI tool-shape translation (must-have, large) ← biggest
  item; sequence early so the rest of v1.1 stabilizes against the
  new shape.
- F-1 `fork --set` (must-have, medium-large).
- F-2 `inspect --memo` (should-have, small) — bundles with F-1
  cheaply.
- F-3 `inspect --search` (should-have, small) — bundles with F-1.
- C-1 Deprecate `graph.query` (should-have, small).

### Phase 2 — Coherence completion + ratchets (should-haves)

- B-3 Pack-load-time cross-provider validation (small).
- B-7 `Runtime.load` auto-provider (medium).
- H-1 Fork cache pre-population symmetry (medium).
- C-2 Graph/View harmonization (medium).
- E-1 Type-completeness (medium).
- E-2 Docstring-completeness Wave 1 (medium).

### Phase 3 — Error-completeness sweep

- A-3 Re-audit Pack* migrations under #4b principle (small).
- A-1 Complete the 22 partial Pack* migrations (medium).
- A-2 DB-error wrappers (medium-large; gated on contributor).

### Phase 4 — Design-pass items and polish

- B-2 Native structured-output mode (medium).
- B-4 Dict-form `output_schema=` support (large).
- B-5 `on_failure=` callback (medium).
- B-6 Fire-once aggregation triggers (medium).
- E-2 Docstring-completeness Wave 2 (large).
- I-2 Boundary-anchored test discipline note (small) — see also
  CONTRACT review banner Standing rule §2 (already in CONTRACT;
  I-2 is the version that lands in `docs/about/discipline.md` or
  HANDOFF.md for project-process docs).
- I-3 Test-file naming sweep (small).

---

---

### Theme J — Storage backend additions

#### STORE-FALKORDB — FalkorDB event-store backend

**Description**: a third `EventStore` implementation alongside SQLite
and Postgres, backed by FalkorDB (Redis-module property graph database
with OpenCypher). The event log is modelled as a small graph
(`(:Run)-[:HAS_EVENT]->(:Event)`); same `EventStore` protocol surface,
same conformance suite. URL form: `falkor://[user:pass@]host[:port]/<graph>`.

**Phasing**:

- **Phase 1 (shipped)**: event-log backend only. Opt-in extra
  `activegraph[falkordb]`, lazy import, conformance-suite parity with
  SQLite/Postgres, three new error snapshots
  (`missing_optional_dependency__falkordb`,
  `invalid_argument_type__falkordb_target`,
  `invalid_store_url__falkordb_no_host`), reference page at
  `docs/reference/event-store-falkordb.md`.
- **Phase 2 (deferred)**: graph-projection backend — materialize the
  runtime graph (objects + typed relations) into FalkorDB so external
  Cypher tooling can query the same world the runtime reasons about.
  Open design questions on coupling (derivative-replay vs.
  live-write-through vs. opt-in-per-run) and conflict semantics. CONTRACT
  amendment expected.
- **Phase 3 (deferred)**: reuse FalkorDB's Cypher engine for the
  behavior-subscription Cypher subset. Out of scope unless Phase 2
  demonstrates demand.

**Source**: 2026-05 user request.

**Dependencies**: none for Phase 1; A-2's DB-error wrappers should
extend to cover FalkorDB connection failures when they land.

**Priority**: should-have. Closes a real-world adoption ask
(operators who already run FalkorDB and want a Cypher-queryable
audit trail) without disturbing existing SQLite/Postgres users.

**Scope**: small (Phase 1), medium-to-large (Phase 2 design + impl).

---

## What v1.1 deliberately does NOT include

Stays consistent with the v1.0 deferral list (CONTRACT v1.0 #10):

- Web UI (never).
- Streaming LLM responses (post-1.0+).
- Multi-model routing / fallback (post-1.0+).
- More packs beyond Diligence (post-1.0+).
- Pack registry or marketplace.
- Error message i18n (never; English only).
- Video tutorials (separate project).
- Adaptive question generation in Diligence (post-1.0 if at all).
- Contradiction resolver in Diligence (post-1.0 if at all).
- Richer fixture cardinality (v0.9 contract is 3 companies).
- `--live` quickstart mode (post-1.0 if quickstart usage shows
  demand).

If a v1.1 PR drifts toward any of these, stop.

---

## Closing

This is a backlog of 26 actionable v1.1 candidates plus 5 already-
shipped or already-closed items kept for completeness. The
should-have / must-have / nice-to-have distribution post-review
sits at roughly: 7 must-haves (B-1, D-1, D-2, D-3, D-4 shipped,
D-5 shipped, F-1, I-1), ~13 should-haves, ~3 nice-to-haves.
The bump from 4 to 7 must-haves came from the contract review's
discipline elevations (D-1, D-2, I-1) — drift gates and CONTRACT
modularization are now phase-1 items because subsequent v1.1
amendments file cleaner against them.

The plan is the consolidation point. CONTRACT.md no longer
scatters "filed for v1.1" markers in amendment bodies; instead,
each deferral reads "Filed in v1.1-plan.md (item X)." See
`CONTRACT-review-findings.md` §7 #3 for the discipline note.

v1.1 scoping begins when this plan and the findings document have
been reviewed by the maintainer and the four open questions above
have answers.
